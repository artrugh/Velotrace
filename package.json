{
  "name": "velotrace",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "lint": "prettier --check '*.{json,yml,yaml,md}' '.github/workflows/*.yml' --log-level debug"
  },
  "devDependencies": {
    "nx": "20.6.0",
    "@nx-go/nx-go": "3.3.1",
    "prettier": "^3.5.2"
  }
}
