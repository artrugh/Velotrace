{
  "name": "identity-api",
  "projectType": "application",
  "targets": {
    "build": {
      "command": "CGO_ENABLED=0 GOOS=linux go build -v -o ../../dist/apps/identity-api/main ./cmd/main.go",
      "options": {
        "cwd": "apps/identity-api"
      }
    },
    "test": {
      "command": "go run apps/identity-api/..."
    },
    "lint": {
      "command": "sh -c '$(go env GOPATH)/bin/golangci-lint run ./... -v'",
      "options": {
        "cwd": "apps/identity-api"
      }
    }
  }
}
